# สรุปการแก้ไขฐานข้อมูลระบบโครงการ

วันที่ดำเนินการ: 6 กันยายน 2569

## วัตถุประสงค์

ปรับโครงสร้างฐานข้อมูลให้แยกโครงการบริการวิชาการออกจากโครงการทำนุบำรุงศิลปวัฒนธรรมอย่างชัดเจน และรองรับการเก็บผลประเมินนักศึกษาตามลักษณะของแต่ละประเภท

## รายการที่แก้ไข

### 1. เพิ่มประเภทโครงการในตาราง `project`

เพิ่มคอลัมน์ `project_type` โดยรองรับค่า:

- `academic_service` — โครงการบริการวิชาการ
- `culture` — โครงการทำนุบำรุงศิลปวัฒนธรรม
- `other` — โครงการประเภทอื่นหรือโครงการเดิมที่ยังไม่ได้จำแนก

กำหนดค่าเริ่มต้นเป็น `other` เพื่อป้องกันการจำแนกโครงการเดิมผิดประเภท และเพิ่มดัชนี `idx_project_type_year` สำหรับค้นหาตามประเภทและปีการศึกษา

โครงการเดิมจำนวน 3 รายการถูกกำหนดเป็น `other` โดยไม่มีการลบหรือเปลี่ยนแปลงรายละเอียดเดิม

### 2. เพิ่มตาราง `project_satisfaction_responses`

ใช้เก็บความพึงพอใจของนักศึกษาที่เข้าร่วมโครงการทำนุบำรุงศิลปวัฒนธรรม ประกอบด้วย:

- โครงการ (`project_id`)
- นักศึกษา (`student_id`)
- ผลความพึงพอใจ (`is_satisfied`)
- ความคิดเห็น (`comment`)
- วันที่ตอบและวันที่แก้ไข

ข้อกำหนดสำคัญ:

- นักศึกษาหนึ่งคนตอบได้หนึ่งครั้งต่อหนึ่งโครงการ
- `is_satisfied` รับเฉพาะค่า `0` หรือ `1`
- ไม่มี Foreign Key โดย Backend ต้องตรวจสอบว่านักศึกษาเป็นผู้เข้าร่วมโครงการและจัดการข้อมูลที่เกี่ยวข้องเมื่อมีการลบ

### 3. เพิ่มตาราง `student_project_outcome_results`

ใช้เก็บผลการประเมิน CLO/PLO/YLO รายคนของนักศึกษาที่เข้าร่วมโครงการบริการวิชาการ ประกอบด้วย:

- โครงการ (`project_id`)
- นักศึกษา (`student_id`)
- CLO/PLO/YLO ที่โครงการกำหนด (`project_outcome_link_id`)
- คะแนนร้อยละ (`score_percent`)
- ผลผ่านหรือไม่ผ่าน (`pass_status`)
- อาจารย์ผู้ประเมิน (`assessed_by`)
- วันที่ประเมินและวันเวลาที่สร้าง/แก้ไขข้อมูล

ข้อกำหนดสำคัญ:

- นักศึกษาหนึ่งคนมีผลได้หนึ่งรายการต่อหนึ่ง CLO/PLO/YLO ในโครงการเดียวกัน
- คะแนนต้องอยู่ระหว่าง 0–100
- `pass_status` รับเฉพาะค่า `0`, `1` หรือ `NULL`
- ต้องระบุคะแนนหรือผลผ่าน/ไม่ผ่านอย่างน้อยหนึ่งค่า
- ไม่มี Foreign Key โดย Backend ต้องตรวจสอบความสัมพันธ์กับผู้เข้าร่วมโครงการ, CLO/PLO/YLO และอาจารย์ผู้ประเมิน

### 4. ปรับดัชนีและข้อกำหนดข้อมูล

- เพิ่ม `idx_project_type_year` ใน `project`
- เพิ่ม unique key ป้องกันคำตอบความพึงพอใจซ้ำ
- เพิ่ม unique key ป้องกันผลประเมิน CLO/PLO/YLO ซ้ำ
- เพิ่ม check constraint สำหรับค่าความพึงพอใจ คะแนน และผลผ่าน/ไม่ผ่าน
- ไม่ใช้ Foreign Key กับสองตารางใหม่ การตรวจสอบความสัมพันธ์และการลบข้อมูลต่อเนื่องต้องดำเนินการใน Backend

## ไฟล์ฐานข้อมูลที่เกี่ยวข้อง

- `MYSQL_DATABASE (3-8-2569).sql` — ปรับฐานข้อมูลฉบับเต็มให้มีโครงสร้างใหม่
- `backend/src/components/Teacher/ProjectsPage/add_project_assessment_schema.sql` — migration สำหรับฐานข้อมูลเดิม

## การนำไปใช้

Migration ถูกนำไปใช้กับฐานข้อมูล MySQL ของโปรเจกต์ที่กำลังทำงานอยู่แล้ว และตรวจพบว่า:

- คอลัมน์ `project.project_type` ถูกสร้างสำเร็จ
- ตาราง `project_satisfaction_responses` ถูกสร้างสำเร็จ
- ตาราง `student_project_outcome_results` ถูกสร้างสำเร็จ
- ดัชนีและ check constraint ถูกสร้างครบ
- ไม่มี Foreign Key ในสองตารางใหม่ตามข้อกำหนดล่าสุด
- ทดสอบเพิ่มข้อมูลผู้เข้าร่วม ความพึงพอใจ และผลประเมินสำเร็จ
- ข้อมูลที่ใช้ทดสอบถูก rollback หรือลบทิ้งแล้ว ไม่มีข้อมูลทดสอบตกค้าง

## การเชื่อมกับ Backend

- API เพิ่มและแก้ไขโครงการรับค่า `project_type` และตรวจสอบค่าที่อนุญาต
- API รายการโครงการส่ง `project_type` ให้ Frontend
- โครงการใหม่เชื่อมอาจารย์ผู้รับผิดชอบจากบัญชีที่เข้าสู่ระบบ
- API เชื่อม CLO/PLO/YLO อนุญาตเฉพาะโครงการบริการวิชาการ
- เมื่อแก้รายการ CLO/PLO/YLO ระบบเก็บผลของรายการเดิมที่ยังใช้อยู่ และลบผลของรายการที่ถูกนำออก
- เพิ่ม API จัดการผู้เข้าร่วม ความพึงพอใจ และผล CLO/PLO/YLO รายคน
- Backend ตรวจสอบความสัมพันธ์แทน Foreign Key ก่อนบันทึกข้อมูล
- เมื่อลบผู้เข้าร่วมหรือโครงการ Backend ลบข้อมูลประเมินที่เกี่ยวข้องเพื่อไม่ให้เกิดข้อมูล orphan
- หน้าภาระงานบริการวิชาการและศิลปวัฒนธรรมอ่านประเภทจาก `project.project_type` โดยตรง

## การเชื่อมกับ Frontend

- หน้าจัดการโครงการมีช่องเลือกและแสดงประเภทโครงการ
- ปุ่มเชื่อม CLO/PLO/YLO แสดงเฉพาะโครงการบริการวิชาการ
- เพิ่มหน้า `ProjectAssessments` สำหรับเพิ่มหรือนำผู้เข้าร่วมออกจากโครงการ
- โครงการศิลปวัฒนธรรมแสดงฟอร์มพึงพอใจ/ไม่พึงพอใจและความคิดเห็น
- โครงการบริการวิชาการแสดงคะแนนและผลผ่าน/ไม่ผ่านแยกตาม CLO/PLO/YLO
- หน้าโครงการของฉันแสดงชื่อประเภทโครงการจากฐานข้อมูล

## ผลการตรวจสอบระบบ

- PHP syntax ผ่านทุกไฟล์ที่แก้ไข
- Frontend production build ผ่าน
- ทดสอบ API โครงการศิลปวัฒนธรรมตั้งแต่สร้างโครงการ เพิ่มผู้เข้าร่วม และบันทึกความพึงพอใจสำเร็จ
- ทดสอบ API โครงการบริการวิชาการตั้งแต่สร้างโครงการ เชื่อม LO เพิ่มผู้เข้าร่วม และบันทึกผลรายคนสำเร็จ
- ทดสอบการเชื่อมอาจารย์ผู้สร้างกับ `responsible_faculty_id` สำเร็จ
- ตรวจสอบหลังลบข้อมูลทดสอบแล้ว ไม่พบโครงการทดสอบหรือข้อมูล orphan ตกค้าง
