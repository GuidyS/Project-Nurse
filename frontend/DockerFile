# 1. เลือก Base Image เป็น Node.js (รุ่น alpine จะมีขนาดเล็ก)
FROM node:18-alpine

# 2. กำหนดพื้นที่ทำงานใน Container
WORKDIR /app

# 3. Copy ไฟล์ package.json เข้าไปก่อน (เพื่อทำ caching layer)
COPY package*.json ./

# 4. ลง Library ต่างๆ (node_modules)
RUN npm install

# 5. Copy โค้ดทั้งหมดเข้าไป
COPY . .

# 6. เปิด Port 3000 (Port มาตรฐานของ React)
EXPOSE 5173

# 7. คำสั่งรันเมื่อ Container เริ่มทำงาน
CMD ["npm", "run", "dev"]